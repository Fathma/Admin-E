<div class="container-fluid" style="width:100%;min-height: 700px">
    <div class="register-logo">
        <a href="#"><b>Product Details</a>
    </div>

    {{!-- {{# if products}} --}}
    <div style="background-color: white">
        <table id="ptable" class="table table-striped table-bordered" style="width:100%">
            <thead>
            <tr>
                <th>#</th>
                <th>Product ID</th>
                <th>Product Name</th>
                <th>Serial Number</th>
                <th>LP number</th>
                <th>Invoice Number</th>
                <th>Status</th>
                {{!-- <th>Action</th> --}}
            </tr>
            </thead>
            <tbody>
            {{# each serials}}
          
            <tr>
                 <td>
                   {{this.count}}
                </td>
                <td>
                   {{this.sid}}
                </td>
                <td>
                   {{this.pid.name}}
                </td>
                
                <td>
                   {{this.number}}
                </td>
                <td>
                   <a href="/purchase/localPurchase/{{this.lp.number}}">{{this.lp.number}}</a>
                </td>
                <td>
                  <a href="/orders/ViewInvoice/{{this.invoice._id}}">{{this.invoice.invoiceId}}</a> 
                </td>
                <td>
                   {{this.status}}
                </td>
                
                {{!-- <td> --}}
                    {{!-- {{# if this.availablity}} --}}
                    {{!-- <a href="/products/serials/replace{{this._id}}" class="btn btn-default ">Edit</a> --}}
                    {{!-- {{else}}
                    <a href="/products/Available/{{this._id}}" class="btn btn-default">Available</a>
                    {{/if}} --}}
                    {{!-- {{# if this.isActive}}
                    <a href="/products/unactive/{{this._id}}" class="btn btn-default ">Disable</a>
                    {{else}}
                    <a href="/products/active/{{this._id}}" class="btn btn-default">Enable</a>
                    {{/if}}
                     <a href="/products/Update/{{this._id}}" class="btn btn-default ">Update</a> --}}
                {{!-- </td> --}}
            </tr>
           
            {{/each}}
            </tbody>
             {{!-- <tfoot>
            <tr>
                <th>Product Name</th>
                <th>Product ID</th>
                <th>Serial Number</th>
                <th>LP number</th>
                <th>Invoice Number</th>
                <th>Status</th>
                <th>Action</th>
            </tr>
             </tfoot> --}}
        </table>
    </div>
</div>